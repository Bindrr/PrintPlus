/* let username
const usernameSubmitButton = document.getElementById('usernameSubmitButton')
usernameSubmitButton.onclick = () => {
  username = document.getElementById('usernameInput').value
  document.getElementById('heading1').textContent = `Welcome, ${username}!`
} */

/* let userAge

while (true){
  let age = window.prompt('How old are you?')
  age = Number(age)
  if (isNaN(age)) {
    alert('Please enter a number')
    continue
  }

  userAge = Number(age)
  break
}

console.log(userAge) */
